{
    "require": {
        "monolog/monolog": "1.0.*",
        "php": "^7.2.0"
    },

    "post-install-cmd": [
        "php artisan clear-compiled",
        "php artisan optimize",
        "chmod -R 777 public/"
    ]
}
